//TODO:  Create audio package; publish to Atmosphere

Package.describe({
  summary: "Adds an audio sound whenever someone clicks a list item."
});

Package.on_use(function (api) {
    //api.add_files('audio.js', 'client/lib');
    //api.add_files('audio.html', 'client/templates');
});
